{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && pnpm exec prettier --write \"$f\" --ignore-unknown 2>/dev/null; } || true",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
